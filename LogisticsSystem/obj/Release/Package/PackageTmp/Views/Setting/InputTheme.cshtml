@model Logistic.Base.Entity.tblTheme
@{
    ViewBag.Title = "InputTheme";
    Layout = "~/Views/Shared/Blank.cshtml";
}

<script src="~/js/jquery-1.11.3.min.js"></script>

<div id="myModal">
    <div class="modal-header">
        <h3 id="myModalLabel">Input Theme</h3>
    </div>
    <div class="modal-body">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <div class="form-group">
                <label>Theme</label>
                @Html.TextBoxFor(m => m.Name, null, new { @class = "form-control" ,id="Theame"})
            </div>
            <div class="form-group">
                <label>Description</label>
                @Html.TextAreaFor(m => m.Description,new { @class = "form-control" ,id="Description"})
            </div>
            <div class="form-group">
                <input type="submit" value="Submit" class="btn btn-primary" id="Search" />
            </div>
        }
    </div>
</div>
<script>
    $(document).ready(function () {
        $('#Search').click(function (e) {
            validateForm(e);
        });

        function validateForm(e) {
            var Theame = $('#Theame').val();
            var Description = $('#Description').val();
            var inputVal = new Array(Theame, Description);

            var inputMessage = new Array("Theme Name", "Description");

            $('.error').hide();

            if (inputVal[0] == "") {
                e.preventDefault()
                $('#Theame').after('<span style="color:red" class="error"> Please enter ' + inputMessage[0] + '</span>');
                return false;
            }
            if (inputVal[1] == "") {
                e.preventDefault()
                $('#Description').after('<span style="color:red" class="error"> Please enter ' + inputMessage[1] + '</span>');
                return false;
            }
            else {
                return true;
            }
        }

    });
</script>

